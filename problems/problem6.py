def generate_subsequences(text):
    if len(text) == 0:
        return ['']
    # TODO complete this function
    # so that it returns a list of all subsequences of the input text
    # e.g. 'abc' -> ['', 'a', 'b', 'c', 'ab', 'ac', 'bc', 'abc']
    raise NotImplementedError
if __name__ == '__main__':
    print(generate_subsequences('abc'))
